---
"@ankhorage/infra": minor
---

Integrate the canonical `supabase-vault` secret-store provider, emit its protected migration through the existing local Supabase lifecycle, add provider diagnostics, and keep bootstrap credentials out of generated public artifacts.
